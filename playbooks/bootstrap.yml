---
# Deploying & Bootstrapping

- hosts: new

  roles:
    - clientsetup
    - common
    - sudoers
    - ntp
    - snmp
    - clamav
    - patching

  post_tasks:
    - command: freshclam
    - service: name=clamd state=restarted
